PORT=3001

# Set to true to use LDAP/AD lookup instead of in-memory sample users.
AD_ENABLED=false
AD_URL=ldap://ad.example.local:389
AD_BIND_DN=CN=SvcUser,OU=Service Accounts,DC=example,DC=local
AD_BIND_PASSWORD=change-me
AD_BASE_DN=OU=Users,DC=example,DC=local
AD_USER_FILTER=(objectClass=user)

